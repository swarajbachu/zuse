<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron + V8 require JIT and writable executable memory under hardened
       runtime. Notarization explicitly allows these for Electron apps. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>

  <!-- node-pty spawns child processes (the user's shell, claude, codex, etc.).
       Without this, fork()/exec() under hardened runtime is blocked. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- Allow the app to read user-selected folders (worktrees, repos) outside
       the sandbox via the open-dialog. We're not in App Sandbox anyway, but
       this is harmless and keeps options open if we later target MAS. -->
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>

  <!-- Outbound network (Anthropic API, GitHub, model providers, update feed). -->
  <key>com.apple.security.network.client</key>
  <true/>
  <key>com.apple.security.network.server</key>
  <true/>

  <!-- The signed desktop helper and iOS app share only a random, synchronized
       local-pairing trust record. Account identity is never inferred from a
       device name or network address. -->
  <key>keychain-access-groups</key>
  <array>
    <string>HMCST4VV42.com.zuse.shared-connectivity</string>
  </array>
</dict>
</plist>
