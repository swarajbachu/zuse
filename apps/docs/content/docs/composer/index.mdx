---
title: Composer and agent control
description: Give agents context, choose execution modes, and steer work in progress.
section: composer
order: 50
platforms: [desktop, browser, mobile]
providers: [claude, codex, grok, gemini, cursor, opencode]
prerequisites: [/start/first-chat]
related: [/composer/modes-permissions, /composer/context-attachments, /composer/subagents, /composer/skills-mcp]
next: /composer/modes-permissions
lastVerified: latest
---


The composer is more than a text box. It binds a prompt to the active provider session, project environment, permission posture, attached context, and submission mode.

Use it to:

- write and edit a request;
- `@`-mention project files;
- attach images and PDFs;
- switch model and provider before a session begins;
- choose plan or normal execution;
- control runtime permissions;
- queue a message while the current turn runs;
- invoke built-in and provider-specific slash commands.

Start with [Modes and permissions](/composer/modes-permissions), then learn [Context and attachments](/composer/context-attachments) and [Skills, commands, and MCP servers](/composer/skills-mcp).
