{
	"buildCommand": "cd ../.. && VITE_ZUSE_HOSTED=1 bun run --filter renderer build",
	"outputDirectory": "dist",
	"framework": "vite",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/index.html"
		}
	]
}
